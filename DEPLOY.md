# Инструкция по развертыванию Telegram бота

Этот документ описывает способы развертывания бота на облачных платформах для постоянной работы.

## 🚀 Варианты развертывания

### 1. Railway (Рекомендуется - простой и быстрый)

**Преимущества:**
- Бесплатный tier с $5 кредитами в месяц
- Простая настройка через GitHub
- Автоматический деплой при push
- Поддержка Docker

**Шаги:**

1. **Создайте аккаунт на Railway:**
   - Перейдите на https://railway.app
   - Войдите через GitHub

2. **Подготовьте проект:**
   ```bash
   # Соберите JAR файл
   mvn clean package -DskipTests
   ```

3. **Создайте новый проект на Railway:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий

4. **Настройте переменные окружения:**
   В настройках проекта добавьте:
   - `TELEGRAM_BOT_TOKEN` = ваш токен бота
   - `TELEGRAM_BOT_USERNAME` = evgeeeeeenbot
   - `TELEGRAM_PRELOAD_CHAT_ID` = ваш chatId (опционально)
   - `SPRING_PROFILES_ACTIVE` = production

5. **Деплой:**
   - Railway автоматически обнаружит Dockerfile
   - При push в GitHub произойдет автоматический деплой

---

### 2. Render (Бесплатный вариант)

**Преимущества:**
- Полностью бесплатный tier
- Автоматический деплой из GitHub
- Простая настройка

**Шаги:**

1. **Создайте аккаунт на Render:**
   - Перейдите на https://render.com
   - Войдите через GitHub

2. **Создайте новый Web Service:**
   - Нажмите "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий

3. **Настройте:**
   - **Name:** telegram-bot
   - **Environment:** Docker
   - **Dockerfile Path:** Dockerfile
   - **Docker Context:** . (точка)

4. **Добавьте переменные окружения:**
   - `TELEGRAM_BOT_TOKEN` = ваш токен
   - `TELEGRAM_BOT_USERNAME` = evgeeeeeenbot
   - `TELEGRAM_PRELOAD_CHAT_ID` = ваш chatId
   - `SPRING_PROFILES_ACTIVE` = production

5. **Деплой:**
   - Нажмите "Create Web Service"
   - Render автоматически соберет и запустит приложение

**Важно:** На бесплатном плане Render "засыпает" после 15 минут неактивности. Для постоянной работы нужен платный план или используйте Railway.

---

### 3. Heroku

**Шаги:**

1. Установите Heroku CLI
2. Войдите: `heroku login`
3. Создайте приложение: `heroku create your-bot-name`
4. Установите переменные окружения:
   ```bash
   heroku config:set TELEGRAM_BOT_TOKEN=ваш_токен
   heroku config:set TELEGRAM_BOT_USERNAME=evgeeeeeenbot
   heroku config:set TELEGRAM_PRELOAD_CHAT_ID=ваш_chatId
   heroku config:set SPRING_PROFILES_ACTIVE=production
   ```
5. Деплой: `git push heroku main`

---

## 📝 Важные моменты

### Переменные окружения

**Обязательные:**
- `TELEGRAM_BOT_TOKEN` - токен вашего бота
- `TELEGRAM_BOT_USERNAME` - username бота

**Опциональные:**
- `TELEGRAM_PRELOAD_CHAT_ID` - для предзагрузки файлов
- `SPRING_PROFILES_ACTIVE=production` - для использования production профиля

### Файлы кэша

Файл `file-id-cache.properties` будет создаваться автоматически на сервере. При перезапуске контейнера кэш сохранится, если используется volume.

### Медиафайлы

Видео файлы (`IMG_0128.MOV`, `IMG_0451.MP4`) должны быть в репозитории в папке `src/main/resources/media/`. Они будут включены в JAR файл при сборке.

---

## 🔧 Локальная сборка и тестирование Docker

```bash
# Сборка JAR
mvn clean package -DskipTests

# Сборка Docker образа
docker build -t telegram-bot .

# Запуск контейнера
docker run -d \
  -e TELEGRAM_BOT_TOKEN=ваш_токен \
  -e TELEGRAM_BOT_USERNAME=evgeeeeeenbot \
  -e TELEGRAM_PRELOAD_CHAT_ID=ваш_chatId \
  -p 8080:8080 \
  --name telegram-bot \
  telegram-bot
```

---

## ✅ Проверка работы

После деплоя:
1. Проверьте логи на платформе
2. Отправьте боту `/start` в Telegram
3. Проверьте, что бот отвечает
4. Проверьте работу кнопок с видео

---

## 🆘 Решение проблем

**Бот не отвечает:**
- Проверьте переменные окружения
- Проверьте логи на платформе
- Убедитесь, что токен правильный

**Видео не отправляется:**
- Проверьте, что файлы есть в `src/main/resources/media/`
- Проверьте логи предзагрузки
- Убедитесь, что `TELEGRAM_PRELOAD_CHAT_ID` установлен

**Приложение падает:**
- Проверьте логи
- Убедитесь, что все зависимости установлены
- Проверьте размер видеофайлов (не должны быть слишком большими)
